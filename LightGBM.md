# 2026.09.05LightGBM(Light Gradient Boosting Machine)

# 核心思路及优点

LightGBM 属于梯度提升树模型。它会按顺序训练一系列小树，每一棵树都尝试修正当前模型还没处理好的部分，最后把这些树组合成一个强模型。

**1. 速度快**：相比于传统的梯度提升算法（比如 XGBoost），LightGBM 训练速度更快。因为它在数据和模型上进行了很多优化，比如使用直方图算法来加速计算。

**2. 内存效率高**：它占用的内存更少，能处理更大规模的数据集。

**3. 精度较强**：在结构化数据上，调参合适时通常能取得很好的预测效果。

**4. 支持并行计算**：可以利用多核 CPU 或者 GPU 来加速训练过程。

**5. 自动处理缺失值**：不需要额外处理数据中的缺失值，LightGBM 会自动进行处理。

**6. 支持分类和回归任务**：不仅可以处理分类问题（比如预测邮件是不是垃圾邮件），也可以处理回归问题（比如预测房价）。

假如你在做预测一名学生考试成绩的模型。首先，你猜这个学生的分数是60分（基础模型）。然后你发现，学生的实际分数比你猜的高了或者低了。于是，你再根据误差调整一下你的预测，比如这次你根据多学了一些特征后，你猜他会得70分。

# 理论基础

**核心：通过梯度提升逐步建立决策树，并通过直方图算法、叶子优先生长等设计提升训练效率。**

### 梯度提升的基本原理

梯度提升（Gradient Boosting）是一种迭代的提升算法，目的是通过**逐步构建弱学习器（通常是决策树）**，并将这些弱学习器**组合**成一个**强学习器**来提高预测性能。

**1. 目标函数**

给定一个数据集 {(xi,yi)}i=1N{(*x**i*,*y**i*)}*i*=1*N*，目标是找到一个函数 F(x)*F*(*x*) 来最小化损失函数 L(y,F(x))*L*(*y*,*F*(*x*))。对于回归问题，常见的损失函数是均方误差：

L(y,F(x))=1N∑i=1N(yi−F(xi))2*L*(*y*,*F*(*x*))=*N*1*i*=1∑*N*(*y**i*−*F*(*x**i*))2

**2. 模型表示**

梯度提升模型表示为：

F(x)=∑m=1Mβmhm(x)*F*(*x*)=*m*=1∑*M**β**m**h**m*(*x*)

其中，hm(x)*h**m*(*x*) 是第 m*m* 棵决策树，βm*β**m* 是其对应的权重。

**3. 梯度提升的迭代过程**

梯度提升算法通过逐步构建新树来拟合前一模型的残差。

- 初始化模型：

F0(x)=arg⁡min⁡β∑i=1NL(yi,β)*F*0(*x*)=arg*β*min*i*=1∑*N**L*(*y**i*,*β*)

- 对于每个迭代 m=1,…,M*m*=1,…,*M*：
- 计算当前模型的残差：

ri,m=−[∂L(yi,F(xi))∂F(xi)]F(x)=Fm−1(x)*r**i*,*m*=−[∂*F*(*x**i*)∂*L*(*y**i*,*F*(*x**i*))]*F*(*x*)=*F**m*−1(*x*)

- 拟合一棵新树 hm(x)*h**m*(*x*) 来预测残差 ri,m*r**i*,*m*。
- 更新模型：

Fm(x)=Fm−1(x)+βmhm(x)*F**m*(*x*)=*F**m*−1(*x*)+*β**m**h**m*(*x*)

其中，βm*β**m* 是步长（学习率），可以通过线搜索确定。

### LightGBM 的改进

LightGBM 在传统的梯度提升算法基础上，进行了以下几方面的改进：

**1. 直方图算法（Histogram-based Algorithm）**：

LightGBM 使用直方图算法将连续特征离散化为 k 个桶，从而减少了计算量。构建直方图的步骤：

- 将每个特征的取值划分为 k 个离散区间（桶）。
- 统计每个区间内的样本数量和梯度的总和。
- 使用直方图加速计算最佳分割点。

**2. 基于叶子的最佳分割（Leaf-wise Growth）**：

传统的梯度提升算法使用层级生长（Level-wise Growth）策略，即在每一层同时分裂所有叶子节点。LightGBM 则使用基于叶子的生长策略，每次分裂**当前损失最大**的叶子节点，这样可以更快地减少误差。

**3. 缺失值处理**：

LightGBM 自动处理缺失值，将缺失值单独作为一个类别，并在训练过程中自动选择最佳分裂方向。

**4. 数据并行和特征并行**：

LightGBM 支持数据并行和特征并行，通过分布式计算加速模型训练。

### 算法流程

**1. 数据预处理**：

- 将连续特征离散化为 k 个桶。
- 处理缺失值，将缺失值单独作为一个类别。

**2. 初始化模型**：

- 初始化常数模型 F0(x)*F*0(*x*)。

**3. 迭代训练**：

- 对每次迭代 m*m*：
  - 计算当前模型的残差 ri,m*r**i*,*m*。
  - 构建直方图，加速寻找最佳分割点。
  - 使用基于叶子的生长策略，选择当前损失最大的叶子节点进行分裂。
  - 更新模型 Fm(x)*F**m*(*x*)。

**4. 输出模型**：

- 训练完成后，输出最终的预测模型 FM(x)*F**M*(*x*)。

LightGBM 的优势主要来自工程优化和树生长策略：直方图算法降低计算成本，叶子优先生长让模型更快降低损失。但叶子优先生长也更容易长出很深的局部分支，所以要配合 `num_leaves`、`max_depth`、`min_data_in_leaf` 等参数控制过拟合。

# 缺失值处理

**LightGBM 可以直接处理特征中的 `NaN`，核心是为缺失值确定分支方向。**

对数值特征，训练时的处理过程是：

1. 尝试一个分裂条件，例如 `年龄 ≤ 30`。
2. 分别计算：**缺失年龄的样本全部进入左分支**、**全部进入右分支**，哪种方式的分裂增益更大。
3. 保留效果更好的分裂及缺失值方向。预测时遇到缺失值，就按该节点记录的方向走。

**方向是每个分裂节点单独确定的**，同一个特征在不同节点可以采用不同方向。

实际价值有两个：

- **利用已有信息**：面积缺失，房子的地段、楼层等信息仍然有用。
- **利用缺失本身的信息**：“没填写面积”这件事，也可能与房价有关。

# 参数优化

```python
# 定义参数网格
param_grid = {
    'regressor__num_leaves': [31, 61, 91],
    'regressor__learning_rate': [0.05, 0.1, 0.2],
    'regressor__n_estimators': [100, 200, 500]
}

# 网格搜索
grid_search = GridSearchCV(pipeline, param_grid, cv=5, scoring='neg_mean_squared_error', verbose=1, n_jobs=-1)
grid_search.fit(X_train, y_train)

# 最佳参数
print("Best parameters found: ", grid_search.best_params_)

# 使用最佳参数进行预测
y_pred_best = grid_search.predict(X_val)

# 评估
rmse_best = np.sqrt(mean_squared_error(y_val, y_pred_best))
r2_best = r2_score(y_val, y_pred_best)

print(f'Optimized RMSE: {rmse_best}')
print(f'Optimized R^2: {r2_best}')
```

|      参数       |                   控制什么                   |                         调大后的影响                         |
| :-------------: | :------------------------------------------: | :----------------------------------------------------------: |
|  `num_leaves`   |        **每棵树最多有多少个叶子节点**        | 能学习更复杂的规律，但更容易过拟合。`91` 比 `31` 的复杂度上限更高 |
| `learning_rate` |       **每棵新树对预测结果的贡献大小**       | 学得更快，但步子过大可能影响效果。`0.1` 表示新树的输出乘以 `0.1` 后加入模型 |
| `n_estimators`  | **提升迭代次数**，普通回归中通常对应树的数量 |          训练更久，拟合能力更强，但过多也可能过拟合          |

# LightGBM的优缺点

## **LightGBM的优点**

**1. 高效性**：LightGBM的训练速度快，适用于处理大规模数据。

**2. 准确性**：LightGBM在预测准确性方面通常表现良好。

**3. 低内存占用**：LightGBM在内存占用方面相对较低，能够处理大规模数据而不会导致内存溢出问题。

**4. 自动处理缺失值**：LightGBM能够自动处理缺失值，简化了数据预处理的流程。

## **LightGBM的缺点**

**1. 容易过拟合**：与其他树模型类似，LightGBM容易过拟合，需要进行适当的正则化。

**2. 对超参数敏感**：LightGBM的性能受超参数的影响较大，需要进行仔细调参。

**3. 不适用于小样本数据**：LightGBM在小样本数据上可能表现不佳，需要足够的数据量才能发挥其优势。
