# 局部加权回归

## 核心思路

==局部加权回归在预测某个点时，会更重视离它近的训练样本，而不是把所有样本一视同仁。==

局部加权回归不是一次求一个全局最优的*θ*，而是：

**每个预测点 x\*x\*，都单独求一个针对它的「局部线性模型」**。

怎么“局部”呢？我们会给每一个训练样本一个**权重 w(i)\*w\*(\*i\*)**，它的大小取决于该样本和预测点 x*x* 的距离。

- 离 x*x* 越近，权重 w(i)*w*(*i*) 越大；
- 离 x*x* 越远，w(i)*w*(*i*) 越小。

## 加权代价函数

给每个点一个权重 w(i)*w*(*i*)，则代价函数变为：

J(θ)=12∑i=1mw(i)(hθ(x(i))−y(i))2*J*(*θ*)=21*i*=1∑*m**w*(*i*)(*h**θ*(*x*(*i*))−*y*(*i*))2

我们仍然要求使 J(θ)*J*(*θ*) 最小的 θ*θ*，不过这时候**「离目标点更近的数据」**的误差对代价函数的影响更大。

## 权重函数的选择（核函数）

局部加权回归常用 **高斯核函数** 计算权重：

\[ w^{(i)}=\exp\left(-\frac{(x-x^{(i)})^T(x-x^{(i)})}{2\tau^2}\right) \]

其中：

- \(x\)：当前要预测的点
- \(x^{(i)}\)：第 \(i\) 个训练样本
- \(w^{(i)}\)：该训练样本的权重
- \(\tau\)：带宽参数，控制“看多远”
- \(\exp(a)=e^a\)

核心规律：

\[ \boxed{\text{距离越近，权重越大}} \]\[ \boxed{\text{距离越远，权重越小}} \]

带宽 \(\tau\)：

- \(\tau\) 小：只重视很近的点，更局部
- \(\tau\) 大：考虑更远的点，更接近全局回归

一句话记忆：

**核函数决定权重，\(\tau\) 决定局部范围。**、

## 最优化推导（正规方程）

局部加权回归的目标是最小化**加权平方误差**：

\[ J(\theta)=\frac12\sum_{i=1}^{m}w^{(i)}\left(\theta^Tx^{(i)}-y^{(i)}\right)^2 \]

其中：

- \(X\)：所有训练样本组成的矩阵
- \(y\)：真实值
- \(W\)：权重矩阵，对角线是每个样本的权重
- \(\theta\)：要求的回归参数

写成矩阵形式：

\[ J(\theta)=\frac12(X\theta-y)^TW(X\theta-y) \]

对 \(\theta\) 求导，并令导数为 0：

\[ X^TW(X\theta-y)=0 \]

最后得到：

\[ \boxed{\theta=(X^TWX)^{-1}X^TWy} \]

这和普通线性回归很像，只是多了一个权重矩阵 \(W\)。

> **不同预测点会产生不同的 \(W\)，所以也会得到不同的 \(\theta\)。**

如果 \(X^TWX\) 不可逆，可以使用伪逆，或者加 Ridge：

\[ \theta=(X^TWX+\lambda I)^{-1}X^TWy \]

一句话记忆：

> **先根据距离计算权重 \(W\)，再用加权最小二乘求当前预测点对应的 \(\theta\)。**

## 完整算法流程

对于一个新输入点 x*x*，想预测其输出 y*y*，算法如下：

1. **输入：**

   - 训练数据 {(x(i),y(i))}i=1m{(*x*(*i*),*y*(*i*))}*i*=1*m*
   - 预测点 x*x*
   - 平滑参数 τ*τ*

2. **构建权重矩阵 W\*W\*：**

   - 对每个 i=1…m*i*=1…*m*，计算：

     w(i)=exp⁡(−(x−x(i))T(x−x(i))2τ2)*w*(*i*)=exp(−2*τ*2(*x*−*x*(*i*))*T*(*x*−*x*(*i*)))

   - 构建对角矩阵 W∈Rm×m*W*∈R*m*×*m*，其中 Wii=w(i)*W**ii*=*w*(*i*)

3. **构造输入矩阵 X\*X\* 和标签 y\*y\***（标准线性回归输入）

4. **计算权重解 θ\*θ\*：**

   θ=(XTWX)−1XTWy*θ*=(*X**T**W**X*)−1*X**T**W**y*

5. **预测输出：**

   y^=θTx*y*^=*θ**T**x*

## 理解重点 & 局部性体现

**局部性体现在哪？**

每次预测都为当前点 x*x* 单独计算一个模型 θ*θ*，训练数据中远离 x*x* 的样本几乎不起作用（因为权重接近 0）。

**和线性回归的区别？**

线性回归训练一次即可，用全体数据；局部加权回归每次预测都重新训练，代价更大但更灵活。

**非参数方法**

局部加权回归常被视为非参数回归方法，因为它不学习一组全局固定参数，而是每次预测时根据局部样本重新拟合。不过它仍然需要我们选择局部模型形式，比如局部线性、局部二次，以及带宽 τ*τ*。

## LWR 的适用场景

你可以优先考虑使用局部加权回归，在以下几种场景中：

1. **数据量较小或中等，计算资源充足**：如科学实验、物理模拟等领域，样本数量有限，且拟合精度要求高。
2. **数据具有复杂、难以建模的局部变化**：如气象变化、医疗时间序列、光谱分析等，整体关系不可预测，但局部趋势明显。
3. **探索性数据分析，需要可解释性强的模型**：每次拟合一个局部模型，可以看到每次预测用到了哪些训练样本及其权重，对理解数据变化很有帮助。
4. **异常值干扰大的环境**：如传感器失真、网络丢包数据，LWR 的局部性降低了异常值影响。

## 何时应考虑其他算法替代

LWR 并不适用于所有情况。

在以下情况中，更应考虑其他模型：

1. **数据规模非常大（> 万级样本）**：推荐使用神经网络、随机森林、XGBoost 等高效全局模型。
2. **对预测效率要求极高（如实时系统）**：LWR 每次预测都要训练模型，延迟高。推荐线性回归、SVR、轻量神经网络。
3. **数据维度非常高（如文本、图像）**：高维空间中距离失去意义，局部性消失。推荐使用深度学习或核方法。
4. **任务目标不仅仅是预测，还包括模型压缩、部署、泛化等工程需求**：LWR 无法“存成模型”，不能部署到设备上。推荐使用支持参数冻结的模型。

# 最后

局部加权回归的核心是“到哪里预测，就在哪里临时拟合一个局部模型”。它很适合样本规模可控、局部趋势明显、我们又希望看清附近样本影响的任务。

真正使用时，重点盯住带宽 *τ*、特征尺度、局部样本数量和矩阵求解稳定性。数据一大、维度一高、或者需要低延迟部署时，它就不太轻松了，这时更适合换成全局模型或近似方法。